/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Note: The 'webpack' configuration for path aliases has been removed.
  // Next.js automatically reads the 'paths' configuration from your tsconfig.json file.
};

export default nextConfig;